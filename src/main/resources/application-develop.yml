server:
  port: 9098
spring:
# 数据库连接
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dreamtour
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#自己的配置信息
myProperties:
  pageSize: 2 #分页大小
